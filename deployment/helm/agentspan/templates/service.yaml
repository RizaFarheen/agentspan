apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentspan.fullname" . }}
  labels:
    {{- include "agentspan.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "agentspan.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: server
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
